#include <stdio.h>
#include <stdlib.h>
#include "lib.h"
#include "types.h"
/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	int i=0;	
//	llamo a la funcion que contiene mi menu
	int valorReturn = menu(i);
}
